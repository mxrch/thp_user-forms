<div align="center" style="margin-top:100px">
  <div class="card text-center" style="width: 30rem;">
    <div class="card-header">
      <h1><%= "#{@user.username}" %></h1>
    </div>
    <div class="card-body">
        <h4><p class="card-text"><%= "Email : #{@user.email}" %></p></h4>
        <h4><p class="card-text"><%= "Bio : #{@user.bio}" %></p></h4>
        <br>
        <p class="card-text"><%= "Date de création : #{@user.created_at}" %></p>
        <p class="card-text"><%= "Dernière mise à jour : #{@user.updated_at}" %></p>
        <br>
        <a href="/users" class="btn btn-primary">Retourner à la liste des utilisateurs</a>
    </div>
  </div>
  <br>
</div>
